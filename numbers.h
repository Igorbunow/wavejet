#ifndef WJ_NUMBERS
#define WJ_NUMBERS

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void nb_eng(double value, int digits, int numeric, char *out);
double nb_engtod(const char *s);

#endif
